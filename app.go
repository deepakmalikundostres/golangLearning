package main // we can change this package but we need to prod we have to tell
// where go to start entry point
// while building the go
// also remember there can we only one function inside a package

func main() {
	bank();
	// profitCalculator()
	// profitCalculatorV2()
}
